# Env vars for the public read-only demo build (demo.ormcrm.com).
# Points at a separate, isolated Supabase project — never the production one.
VITE_SUPABASE_URL=https://<demo-project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<demo project's anon/publishable key>
VITE_DEMO_MODE=true
